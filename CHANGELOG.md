# Changelog

## Unreleased

### contextharbor
- Deep research pipeline hardening (offline-first defaults, evidence/citation gating, async run endpoint).
- RAG/library improvements (document tags, exclude filters, safer EPUB defaults).
- Web search improvements (SearxNG support + safer DDG fallback behavior).

### ollama-cli
- Research pipeline reliability improvements (retry + tool loop refinements).
